# 接口文档

> **版本**：v1.0  
> **更新**：2026-07-26  
> **Base URL**：`/api/v1`

---

## 一、全局规范

### 1.1 鉴权方式

JWT Bearer Token：
```
Authorization: Bearer {token}
```

### 1.2 统一响应体

```json
{
  "code": 200,
  "message": "success",
  "data": {}
}
```

### 1.3 错误码定义

| code | 含义 | 触发场景 |
|------|------|----------|
| 200 | 成功 | — |
| 401 | 未登录 | Token 缺失 / 过期 / 无效 |
| 403 | 无权限 | 越权操作 |
| 404 | 资源不存在 | 查询实体不存在 |
| 429 | 请求过于频繁 | 接口限流触发 |
| 500 | 服务器错误 | 未捕获异常 |

### 1.4 分页规范

**请求参数：**

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| page | int | 1 | 页码，从 1 开始 |
| size | int | 20 | 每页条数 |

**分页响应格式：**

```json
{
  "records": [],
  "total": 100,
  "page": 1,
  "size": 20
}
```

---

## 二、接口清单（24 个）

图例：√ 需要，× 不需要

### 2.1 用户模块（5 个）

| 方法 | 路径 | 说明 | 鉴权 |
|------|------|------|:---:|
| POST | `/api/v1/user/login` | 微信登录 | × |
| GET | `/api/v1/user/profile` | 获取个人信息 | √ |
| PUT | `/api/v1/user/profile` | 更新个人信息 | √ |
| PUT | `/api/v1/user/preference` | 更新偏好设置 | √ |
| GET | `/api/v1/user/preference` | 获取偏好设置 | √ |

### 2.2 菜谱模块（4 个）

| 方法 | 路径 | 说明 | 鉴权 |
|------|------|------|:---:|
| GET | `/api/v1/recipe/search` | 多条件搜索菜谱 | × |
| GET | `/api/v1/recipe/hot` | 热门推荐 Top10 | × |
| GET | `/api/v1/recipe/{id}` | 菜谱详情（含用料+步骤） | × |
| GET | `/api/v1/recipe/{id}/steps` | 获取步骤列表 | × |

### 2.3 推荐模块（3 个）

| 方法 | 路径 | 说明 | 鉴权 |
|------|------|------|:---:|
| GET | `/api/v1/recommend/personal` | 个性化推荐 | √ |
| GET | `/api/v1/recommend/daily` | 今日吃什么 | √ |
| GET | `/api/v1/recommend/by-ingredients` | 按食材匹配菜谱 | √ |

### 2.4 AI 模块（6 个）

| 方法 | 路径 | 说明 | 鉴权 |
|------|------|------|:---:|
| POST | `/api/v1/ai/generate` | AI 生成菜谱 | √ |
| POST | `/api/v1/ai/generate/stream` | AI 流式生成菜谱（SSE） | √ |
| GET | `/api/v1/ai/history` | 生成历史列表 | √ |
| GET | `/api/v1/ai/history/{id}` | 生成历史详情 | √ |
| POST | `/api/v1/ai/history/{id}/save` | 保存到收藏 | √ |
| POST | `/api/v1/ai/feedback` | 评价生成结果 | √ |

### 2.5 收藏模块（4 个）

| 方法 | 路径 | 说明 | 鉴权 |
|------|------|------|:---:|
| POST | `/api/v1/favorite` | 添加收藏 | √ |
| DELETE | `/api/v1/favorite/{recipeId}` | 取消收藏 | √ |
| GET | `/api/v1/favorite/list` | 收藏列表 | √ |
| GET | `/api/v1/favorite/check/{recipeId}` | 检查是否已收藏 | √ |

### 2.6 搜索历史（2 个）

| 方法 | 路径 | 说明 | 鉴权 |
|------|------|------|:---:|
| GET | `/api/v1/search/history` | 最近搜索历史 | √ |
| DELETE | `/api/v1/search/history` | 清空搜索历史 | √ |

---

## 三、接口详细设计

### 3.1 用户模块

#### POST `/api/v1/user/login` — 微信登录

```
Request:
{
  "code": "微信登录code"
}

Response (200):
{
  "code": 200,
  "message": "success",
  "data": {
    "token": "eyJhbGciOi...",
    "userInfo": {
      "id": 1,
      "nickname": "用户昵称",
      "avatar": "https://..."
    }
  }
}
```

#### GET `/api/v1/user/profile` — 获取个人信息

```
Response (200):
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "nickname": "用户昵称",
    "avatar": "https://...",
    "phone": "13800138000",
    "gender": 1
  }
}
```

#### PUT `/api/v1/user/preference` — 更新偏好

```
Request:
{
  "taste": "麻辣",
  "taboo": "[\"香菜\",\"芹菜\"]",
  "cuisine": "[\"川菜\",\"粤菜\"]",
  "difficulty": "普通",
  "cookMethod": "[\"炒\",\"蒸\"]"
}
```

---

### 3.2 菜谱模块

#### GET `/api/v1/recipe/search` — 多条件搜索

```
Query Parameters:
  keyword    string   搜索关键词
  cuisine    string   菜系
  difficulty string   难度
  cookMethod string   烹饪方式
  page       int      页码
  size       int      每页条数

Response (200):
{
  "code": 200,
  "message": "success",
  "data": {
    "records": [
      {
        "id": 1,
        "name": "番茄炒蛋",
        "coverImage": "https://...",
        "description": "经典家常菜...",
        "cuisine": "家常",
        "difficulty": "简单",
        "cookMethod": "炒",
        "cookTime": 15,
        "calories": 180,
        "favoriteCount": 128
      }
    ],
    "total": 100,
    "page": 1,
    "size": 20
  }
}
```

#### GET `/api/v1/recipe/{id}` — 菜谱详情

```
Response (200):
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "name": "番茄炒蛋",
    "coverImage": "https://...",
    "description": "经典家常菜，简单易做",
    "cuisine": "家常",
    "difficulty": "简单",
    "cookMethod": "炒",
    "cookTime": 15,
    "calories": 180,
    "viewCount": 1024,
    "favoriteCount": 128,
    "ingredients": [
      { "name": "番茄", "amount": "2个" },
      { "name": "鸡蛋", "amount": "3个" },
      { "name": "盐", "amount": "适量" },
      { "name": "糖", "amount": "少许" }
    ],
    "steps": [
      { "stepNo": 1, "content": "番茄洗净切块...", "image": "https://...", "duration": 2 },
      { "stepNo": 2, "content": "鸡蛋打散...", "image": "https://...", "duration": 1 }
    ]
  }
}
```

#### GET `/api/v1/recipe/hot` — 热门推荐

```
Response (200):
{
  "code": 200,
  "message": "success",
  "data": [
    { "id": 1, "name": "番茄炒蛋", "coverImage": "...", "favoriteCount": 128 },
    ...
  ]  // 固定返回 Top 10
}
```

---

### 3.3 AI 模块

#### POST `/api/v1/ai/generate` — AI 生成菜谱

```
Request:
{
  "mode": "ingredients",
  "ingredients": ["鸡蛋", "番茄", "葱"],
  "conditions": "少油少盐"
}

Response (200):
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 42,
    "recipes": [
      {
        "name": "番茄炒蛋",
        "cuisine": "家常",
        "difficulty": "简单",
        "cookTime": 15,
        "ingredients": [
          { "name": "番茄", "amount": "2个" },
          { "name": "鸡蛋", "amount": "3个" }
        ],
        "steps": [
          { "stepNo": 1, "content": "..." },
          { "stepNo": 2, "content": "..." }
        ]
      }
    ],
    "fromCache": false
  }
}
```

**生成模式说明：**

| mode | 参数 | 说明 |
|------|------|------|
| `ingredients` | ingredients (List) | 根据食材生成可行菜谱 |
| `name` | name (String) | 根据菜名补全用料与步骤 |
| `creative` | cuisineA + cuisineB | 融合两种菜系生成创意菜谱 |

**限流与缓存：**
- 会员/每日次数限制已移除，所有功能开放
- 相同输入 24h 内走 Redis 缓存
- API 不可用时返回兜底菜谱

#### POST `/api/v1/ai/generate/stream` — AI 流式生成（SSE）

```
请求参数与 /api/v1/ai/generate 一致。
```

返回 `text/event-stream`，事件如下：

| 事件 | 数据 | 说明 |
|------|------|------|
| `delta` | `{ "content": "..." }` | 推荐文字增量 |
| `done` | `{ "code": 200, "data": { "id": 42, "recipes": [...] } }` | 生成完成，携带菜谱卡片 |
| `error` | `{ "message": "..." }` | 生成失败 |

---

### 3.4 收藏模块

#### POST `/api/v1/favorite` — 添加收藏

```
Request:
{
  "recipeId": 1
}

Response (200):
{
  "code": 200,
  "message": "收藏成功",
  "data": null
}
```

#### DELETE `/api/v1/favorite/{recipeId}` — 取消收藏

```
Response (200):
{
  "code": 200,
  "message": "已取消收藏",
  "data": null
}
```

---

---

## 四、接口限流规则

| 接口 | 限流 | Redis Key |
|------|------|-----------|
| AI 生成 / AI 流式生成 | 当前不限次（会员限制已移除） | 不使用 `jtcsm:ai:limit:*` |
