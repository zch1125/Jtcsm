# ========================================================
# Nacos 引导配置
# 业务配置统一存放在 Nacos（jtcsm-admin.yaml + jtcsm-admin-{profile}.yaml）
# ========================================================
spring:
  application:
    name: jtcsm-admin
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  cloud:
    nacos:
      config:
        server-addr: ${NACOS_SERVER_ADDR:localhost:8848}
        username: ${NACOS_USERNAME:nacos}
        password: ${NACOS_PASSWORD:nacos}
        file-extension: yaml
        timeout: 5000
  config:
    import:
      - "nacos:jtcsm-admin.yaml?refreshEnabled=true&group=DEFAULT_GROUP"
---
spring:
  config:
    activate:
      on-profile: dev
    import: "nacos:jtcsm-admin-dev.yaml?refreshEnabled=true&group=DEFAULT_GROUP"
---
spring:
  config:
    activate:
      on-profile: uat
    import: "nacos:jtcsm-admin-uat.yaml?refreshEnabled=true&group=DEFAULT_GROUP"
---
spring:
  config:
    activate:
      on-profile: pro
    import: "nacos:jtcsm-admin-pro.yaml?refreshEnabled=true&group=DEFAULT_GROUP"
